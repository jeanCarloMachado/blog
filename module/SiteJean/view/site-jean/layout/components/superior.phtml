<?php require_once __DIR__ . '/head.phtml'; ?>

<body>
	<header class="container">


		<div id="topo" class="row">
			<div class="col-xs-12 col-md-4">
				<!-- <a href="<?php echo $this->url('dashboard'); ?>" title="Controla car"><img src="<?php echo $this->basePath("/site-jean/img/logo.png"); ?>" alt="Controla car" class="img-logo" /></a> -->
			</div>
            <div class="col-xs-12 col-md-8">
                <br class="visible-md visible-lg" />
                <br class="visible-md visible-lg" />
                <h1></h1>
            </div>



		</div>

		<!-- Menu de informações genéricas do sistema importantes para o usuário as quais devem permanecer em todas as páginas -->
		<?php echo $this->partial("/site-jean/layout/partials/mainMenu.phtml"); ?>

        <?php
            $sm = $this->getHelperPluginManager()->getServiceLocator();
            $sm = $this->getHelperPluginManager()->getServiceLocator();
            $routeMatch = $sm->get('Application')->getMvcEvent()->getRouteMatch();

            if($routeMatch->getMatchedRouteName() != 'home' ) {
				echo $this->partial("/site-jean/layout/partials/breadCrumbs.phtml");
            }
		?>
	</header>





	<section role="main" class="container">
        <?php

            $messages = (isset($messages)) ? $messages : array();

            $notifications = $this->notificationBlock($messages);
            if ($notifications->hasMessages()):
        ?>
                <div class="row">
                    <div class="col-xs-12 hidden-print">
                    <?php
                        echo $notifications->render();
                    ?>
                    </div>
                </div>
        <?php endif; ?>
